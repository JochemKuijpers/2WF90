package main;

public class Runner {
	public static void main(String[] args) {
		// ...
		// Awesome code here
	}
	
	
	/**
	 * Gets an input integer from command line, and returns it with each digit in the corrseponding
	 * location in the output array. 
	 * It can read hexadecimal input but it will return it to the 'decimal' value, A = 10..
	 * @return Integer array containing the input separated per digit.
	 */
	int[] getInput(){
		
		
		return null;
	}
	
	/**
	 * Reads an hexadecimal input, and returns it to a decimal, so A = 10...
	 * @return Decimal value of the input read from command line
	 */
	int getBase(){
		
		return 0;
	}
}
